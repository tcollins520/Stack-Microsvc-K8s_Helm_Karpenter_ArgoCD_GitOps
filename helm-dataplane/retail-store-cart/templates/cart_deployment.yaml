apiVersion: apps/v1
kind: Deployment

metadata:
  name: carts

  labels:
    app.kubernetes.io/name: carts
    app.kubernetes.io/instance: carts
    app.kubernetes.io/component: service
    app.kubernetes.io/owner: retail-store-app

spec:

{{- if not .Values.autoscaling.enabled }}
  replicas: {{ .Values.replicaCount }}
{{- end }}

  strategy:
    rollingUpdate:
      maxUnavailable: 1
    type: RollingUpdate

  selector:
    matchLabels:
      app.kubernetes.io/name: carts
      app.kubernetes.io/instance: carts
      app.kubernetes.io/component: service
      app.kubernetes.io/owner: retail-store-app

  template:

    metadata:

      annotations:
        prometheus.io/path: /metrics
        prometheus.io/port: "8080"
        prometheus.io/scrape: "true"

      labels:
        app.kubernetes.io/name: carts
        app.kubernetes.io/instance: carts
        app.kubernetes.io/component: service
        app.kubernetes.io/owner: retail-store-app

    spec:

      topologySpreadConstraints:

        - maxSkew: 1
          topologyKey: kubernetes.io/hostname
          whenUnsatisfiable: ScheduleAnyway
          labelSelector:
            matchLabels:
              app.kubernetes.io/name: carts
              app.kubernetes.io/component: service
              app.kubernetes.io/instance: carts

        - maxSkew: 1
          topologyKey: topology.kubernetes.io/zone
          whenUnsatisfiable: ScheduleAnyway
          labelSelector:
            matchLabels:
              app.kubernetes.io/name: carts
              app.kubernetes.io/component: service
              app.kubernetes.io/instance: carts

      serviceAccountName: {{ .Values.serviceAccount.name }}

      securityContext:
        fsGroup: 1000

      containers:

        - name: cart

          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}

          envFrom:
            - configMapRef:
                name: carts

          securityContext:
            capabilities:
              drop:
                - ALL
            readOnlyRootFilesystem: true
            runAsNonRoot: true
            runAsUser: 1000

          ports:
            - name: http
              containerPort: {{ .Values.carts.containerPort }}
              protocol: TCP

          readinessProbe:
            httpGet:
              path: {{ .Values.carts.probes.readiness.path }}
              port: {{ .Values.carts.containerPort }}

            initialDelaySeconds: {{ .Values.carts.probes.readiness.initialDelaySeconds }}
            periodSeconds: {{ .Values.carts.probes.readiness.periodSeconds }}

          livenessProbe:
            httpGet:
              path: {{ .Values.carts.probes.liveness.path }}
              port: {{ .Values.carts.containerPort }}

            initialDelaySeconds: {{ .Values.carts.probes.liveness.initialDelaySeconds }}
            periodSeconds: {{ .Values.carts.probes.liveness.periodSeconds }}

          resources:
{{ toYaml .Values.carts.resources | indent 12 }}

          volumeMounts:

            - mountPath: /tmp
              name: tmp-volume

      volumes:
        - name: tmp-volume
          emptyDir:
            medium: Memory