# Source: retail-store-app-ui-chart/templates/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: ui-np
  labels:
    app: ui
spec:
  #type: ClusterIP  
  type: NodePort
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: ui