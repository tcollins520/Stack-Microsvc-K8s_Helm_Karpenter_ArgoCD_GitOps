# Source: retail-store-app-checkout-chart/templates/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: checkout
  labels:
    app: retail-store-app
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: tcp
      name: http
  selector:
    app: checkout
